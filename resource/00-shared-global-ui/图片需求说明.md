# 共用 UI 图片需求（多页复用）

以下资源被 **`SiteHeader.vue`**、**`ItheimaWwwPageChrome.vue`** 等组件引用，换图后影响**所有挂载这些组件的页面**。

**统一命名**：`p00-{组件简写}-{用途}-{序号}.{扩展名}`，扩展名建议 **`.webp`** 或 **`.png`**（需透明用 PNG）。

| 序号 | 用途说明 | 建议规格 | 命名示例 | 代码位置（替换时） |
|------|----------|----------|----------|---------------------|
| 1 | 视频库顶栏品牌 Logo（白底条左侧） | 约 **280×96** px（@2x），显示宽约 140px | `p00-sh-logo-01.webp` | `src/components/SiteHeader.vue` → `PlaceholderImage` `ratio="140 / 48"` |
| 2 | 顶栏用户头像占位 | **56×56** px 方图（@2x），圆角由 CSS 裁切 | `p00-sh-user-01.webp` | `SiteHeader.vue` → `ratio="1"` `site-head__user-ico` |
| 3 | 官网壳（师资/开班/学费/就业）顶栏 Logo | 约 **320×96** px（@2x） | `p00-iwc-logo-01.webp` | `ItheimaWwwPageChrome.vue` → `ratio="160 / 48"` |

**说明**：项目主 Logo 源文件为 **`resource/logo.png`**，代码中通过 **`src/assets/brand.js`** 导入，组件 **`src/components/BrandLogo.vue`** 统一引用（`variant`: `header` | `mark` | `compact` | `avatar`）。浏览器标签图标使用 **`public/logo.png`**（与源文件同步复制）。

**已接入页面/组件**（2026-05-14）：

| 位置 | variant | 文件 |
|------|---------|------|
| 视频库顶栏 Logo | `header` | `SiteHeader.vue` |
| 顶栏用户位小图 | `avatar` | `SiteHeader.vue` |
| 官网壳顶栏 | `header` | `ItheimaWwwPageChrome.vue` |
| 门户首页圆标 | `mark` | `ItheimaPortalView.vue` |
| 学习路线图顶栏 | `compact` | `SubjectAiappmapView.vue` |
| AI 培训落地页顶栏 | `compact` | `AiappLandingView.vue` |
| 产品经理地图下载条 | `header` | `MapPmView.vue` |
| 浏览器 favicon | — | `index.html` → `/logo.png` |
| 官网门户首屏壳图 | — | `ItheimaPortalView.vue` Hero 背景，源文件 **`home_shark.png`**，常量 `HOME_SHELL_BG_URL` |
| 学生风采配图 | — | `ItheimaPortalView.vue`「学生风采」区，源文件 **`students.png`**，常量 `STUDENTS_SHOWCASE_URL` |
