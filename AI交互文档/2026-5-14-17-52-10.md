# 交互记录 `2026-5-14-17-52-10`

## 用户问题（原文）

> 请你把我接下来的全部问题进行记录标题命名为当前的24小时制时间戳，记录内容包括我问的问题的原文和你回答的摘要，记录到@AI交互文档 这个文档里面，请你帮我整理我整个项目的所有网页把所有网页每一个网页一个文件夹放到@白马程序员内部/resource 里面我要开始找图片进行匹配了每个网页下面多加入一个文档上面写明这个网页需要哪些图片，明明的方式是什么，可以以表格的形式呈现

## 回复摘要

1. **交互记录**：已按当前本地时间戳 **`2026-5-14-17-52-10`** 在本文件记录本条提问与摘要。说明：「接下来全部问题」需在每次对话后**继续新建**同格式时间戳文件（或由你指定周期合并）；本次仅覆盖**当前这一条**用户消息。

2. **资源目录整理**：在 **`白马程序员内部/resource/`** 下为 **16 个网页路由** 各建独立子文件夹（另增 **`00-shared-global-ui`** 存放多页共用的顶栏 Logo、用户头像等说明），与 `src/router/index.js` 中的页面对齐。

3. **每页配套文档**：每个网页文件夹内新增 **`图片需求说明.md`**，以 **Markdown 表格**列出：用途、建议尺寸/比例、**统一命名规范**、建议文件名、在 Vue 中的替换位置（`PlaceholderImage` 或组件路径）。命名规则示例：`{页码}-{区块}-{序号}.png`（如 `p02-hero-01.jpg`）。

4. **后续**：将真实图片放入对应文件夹后，需在代码中把 `PlaceholderImage` 改为 `<img :src="...">` 或给占位组件增加 `src` 属性（本次仅建目录与说明文档，**未改业务代码**）。

## 维护方式（「接下来的问题」如何记录）

- 每条新问题建议新建文件：`AI交互文档/yyyy-M-d-H-mm-ss.md`（与现有 `2026-5-13-14-21-06.md` 同类），内容为：**问题原文** + **助手回复摘要**。
- 若你希望**追加到同一文件**，可在提问时说明「追加到 2026-5-14-17-52-10」。
