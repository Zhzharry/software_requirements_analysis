# 页面：AI 智能应用学习路线图 `/subject/aiappmap`

- **路由**：`/subject/aiappmap`（及重定向 `/subject/aiappmap/index.html`）
- **视图**：`src/views/SubjectAiappmapView.vue`

**命名规则**：`p16-{区块}-{序号}.webp`

| 序号 | 用途说明 | 建议规格 / 比例 | 命名示例 | 对应代码 |
|------|----------|-------------------|----------|----------|
| 1 | 顶栏 Logo（可与共用 Logo 同源导出） | **240×80**（120×40 @2x） | `p16-head-logo-01.webp` | `ratio="120 / 40"` `aimap__logo` |

**说明**：若与 `00-shared-global-ui` 中 Logo 统一，可复制同文件并保留 `p16-*` 命名一份，避免路径歧义。
