# 2026-5-13-19-00-00

> 磁盘文件名：`2026-5-13-14-21-06.md`  
> 说明：本条为**本轮用户原文**与**助手实现摘要**；并汇总近期与本大作业相关的多轮需求，便于与仓库代码对照。

---

## 用户问题（原文 · 本轮）

### 一、报考大学外链

报考大学这个按钮直接跳转到北京邮电大学的官网上即可 `https://www.bupt.edu.cn/` 不用仿照，直接跳转到这个网页即可

### 二、交互文档归档

请你整理上述多个问题的我的提问文本和你的回答总结，然后将文本信息存入 `@AI交互文档` 这个里面。

---

## 助手回复摘要（本轮实现）

1. **「报考大学」→ 北京邮电大学官网（外站直链，不仿页）**  
   - **`ItheimaPortalView.vue`**：`topMenus` 中该项改为 `external: 'https://www.bupt.edu.cn/'`；主导航模板增加 **`v-if="m.external"`** 分支，渲染 **`<a class="portal__menu-item portal__menu-item--link" href="…" target="_blank" rel="noopener noreferrer">`**。  
   - **`ItheimaWwwPageChrome.vue`**：`mainNav` 中「报考大学」使用 **`external`**；导航模板中 **`v-if="n.external"`** 渲染同上属性的 **`<a class="iwc__nav-a">`**。  
   - 在师资 / 开班 / 学费 / 就业等使用顶栏的页面，点击「报考大学」会在**新标签**打开 [北京邮电大学官网](https://www.bupt.edu.cn/)。

2. **本文档**  
   按既有约定写入 `AI交互文档`，文件名采用本地日期时间样式；下文为**近期多轮需求合并回顾**（非每一字均来自同一条用户消息，敏感/系统提示已省略）。

---

## 附：近期多轮需求与实现对照（合并整理）

| 主题 | 用户意图（概括） | 实现要点（仓库） |
|------|------------------|------------------|
| 师资力量 | 新标签打开站内页，仿 `teacher.html` | `/teacher`，`TeacherView.vue` + 门户 `blank`；后与 `ItheimaWwwPageChrome` 统一顶栏 |
| 开班详情 | 仿校区开班分布页 | `/school`，`ItheimaSchoolView.vue` |
| 学费价格 | 仿学费表与新学员区 | `/price`，`ItheimaPriceView.vue` |
| 就业服务 | 仿 `service.html` | `/service`，`ItheimaServiceView.vue` |
| 报考大学 | 直跳北邮官网、不仿站 | 外链 `https://www.bupt.edu.cn/`，见本轮改动 |
| 视频库与多页复刻 | Vue3 原型、地图页、文章/工具/公开课等 | `HomePrototype`、`MapPmView`、`MapJavaView`、`OpenLiveView`、`JishuView`、`GongjuView` 及路由等（详见各日期的 `AI交互文档` 单条） |
| 课程详情复刻 | 站内 `/course/1002` 等 | `CourseDetailView.vue`、`courseDetails.js`，见 `2026-5-12-19-25-00.md` |
| 官网首页与门户 | `www.itheima.com` 结构 | `/` → `ItheimaPortalView.vue`，视频库 `/yun` |
| AI 培训落地页等 | `aiapp`、学习路线图页等 | `/aiapp`、`SubjectAiappmapView` 等 |

**路由速查（部分）：** `/` 门户，`/yun` 视频库，`/teacher` ` /school` ` /price` ` /service` 官网向子页，`/map/22` ` /map/96` 教程聚合，`/course/:id` 课程详情，`/open` ` /jishu` ` /gongju` 等。

---

## 说明

- 更早的逐轮原文与摘要仍保留在同目录下其它 **`2026-5-11-*`**、**`2026-5-12-*`**、**`2026-5-13-12-00-00.md`** 等文件中；需要「每一轮一字不差」时可与 Cursor **会话 transcript** 及上述文件交叉查阅。  
- 北邮官网内容以北邮站点为准；本项目仅提供导航外链，不镜像其页面。
