# 页面：AI 应用落地页 `/aiapp`

- **路由**：`/aiapp`
- **视图**：`src/views/AiappLandingView.vue`
- **图片目录（代码读取此路径）**：`resource/page-15-aiapp-landing-AI应用落地页/`

支持 `.webp` 或 `.png`。**新增或移动图片后请重启 `npm run dev`**。

| 文件名（放入本目录） | 页面位置 |
|---------------------|----------|
| `p15-hero-01.webp` 或 `.png` | 首屏 Hero 右侧主图 |

**说明**：行业 Tab 左侧示意图、底部「双轨能力」区块已在页面内用 **SVG 简单几何拼贴**（`AiappGeomCollage.vue`）代替，**不再需要** `p15-industry-*`、`p15-section-*` 配图文件。
