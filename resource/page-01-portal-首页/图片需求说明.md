# 页面：官网门户首页 `/`

- **路由**：`/`
- **视图**：`src/views/ItheimaPortalView.vue`

**命名规则**：`p01-{区块}-{序号}.webp`（透明/插画可用 `.png`）

| 序号 | 用途说明 | 建议规格 / 比例 | 命名示例 | 对应代码（占位） |
|------|----------|-------------------|----------|-------------------|
| 1 | 学生风采 / 学员案例横条（占位横幅） | 约 **2400×640** px 或 **1200×320** 比例 | `p01-showcase-01.webp` | `PlaceholderImage ratio="1200 / 320"` |
| 2 | （可选）圆形品牌图标，替换 CSS 渐变圆 | **96×96** px | `p01-brand-mark-01.png` | 当前为 `.portal__logo-mark` 纯 CSS，若改 `<img>` 需改模板 |

**顶栏文字 Logo**：当前无 `PlaceholderImage`；若要用图，建议放本目录 `p01-wordmark-01.webp` 并在 `portal__brand` 内增加 `<img>`。
