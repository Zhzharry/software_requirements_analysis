# 2026-5-13-14-23-53

> 磁盘文件名：`2026-5-13-14-23-53.md`（生成时本地时间）

---

## 用户问题（原文）

### 一、`/open` 页「报考大学」外链

`http://localhost:5173/open` 报考大学这个也要跳转到同样的页面，不用仿照，直接跳转到这个网页即可

（「同样的页面」指北京邮电大学官网：`https://www.bupt.edu.cn/`。）

### 二、写入 AI 交互文档

请你整理上述多个问题的我的提问文本和你的回答总结，然后将文本信息存入 `@AI交互文档` 这个里面命名用当前的时间即可。

---

## 助手回复摘要

### 1. 视频库顶栏「报考大学」→ 北邮官网

- **`src/navigation.js`**：新增常量 **`COLLEGE_BUPT_URL = 'https://www.bupt.edu.cn/'`**；**`mainNavItems`** 中「报考大学」由 `to: null` 改为 **`external: COLLEGE_BUPT_URL`**。  
- **`src/components/SiteHeader.vue`**：在主导航循环中增加 **`v-else-if="item.external"`** 分支，渲染 **`<a class="site-head__nav-item site-head__nav-item--link" … target="_blank" rel="noopener noreferrer">`**。  
- **`/open`（`OpenLiveView`）** 与其它使用 **`SiteHeader`** 的页面（如 `/yun`、`/map/22` 等）共用同一主导航，因此 **直播公开课页** 点击「报考大学」会在**新标签**打开 [北京邮电大学官网](https://www.bupt.edu.cn/)，无需再仿页。

### 2. 与门户、官网子站顶栏统一出口（顺带整理）

- **`ItheimaPortalView.vue`**、**`ItheimaWwwPageChrome.vue`** 中北邮地址改为 **引用 `COLLEGE_BUPT_URL`**，避免多处硬编码。

### 3. 验证

- 本地打开 **`http://localhost:5173/open`**，点击主导航 **「报考大学」** 应新开北邮官网。  
- 已执行 **`npm run build`** 通过（若你本地未跑，可在 `白马程序员内部` 目录执行一次确认）。

---

## 附：本轮与「报考大学」相关的前序约定（简要）

- 门户首页 **`/`** 与 **`ItheimaWwwPageChrome`** 下「报考大学」此前已改为外链北邮；本轮补齐 **视频库体系（`SiteHeader` + `navigation.js`）** 的同行为。
