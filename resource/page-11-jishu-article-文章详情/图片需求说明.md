# 页面：技术文章详情 `/jishu/article/:id`

- **路由**：`/jishu/article/:id`（示例 `452`）
- **视图**：`src/views/JishuArticleView.vue`
- **图片目录**：`白马程序员内部/resource/page-11-jishu-article-文章详情/`

| 文件名（放入本目录） | 页面位置 |
|---------------------|----------|
| `p11-default-course-01.webp` | 侧栏推荐课程封面（默认） |
| `p11-452-course-01.webp` | （可选）文章 id=452 专用课程图 |

未放 `p11-{id}-course-01` 时使用 `p11-default-course-01`。
